<UserControl x:Class="FlopsNewProjectTemplate.Views.HomeView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:local="clr-namespace:FlopsNewProjectTemplate.Views"
            xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
            xmlns:appControls="clr-namespace:FlopsNewProjectTemplate.Controls"
            xmlns:viewModel="clr-namespace:FlopsNewProjectTemplate.ViewModel"
            xmlns:views="clr-namespace:FlopsNewProjectTemplate.Views"
            TextElement.Foreground="{DynamicResource MaterialDesignBody}"
            Background="{DynamicResource MaterialDesignPaper}"
            TextElement.FontWeight="Medium"
            TextElement.FontSize="14"
            FontFamily="{materialDesign:MaterialDesignFont}"
            mc:Ignorable="d"
            d:DataContext="{d:DesignInstance viewModel:HomeViewModel, IsDesignTimeCreatable=True}"
            d:Height="400" d:Width="800">
    <Grid Margin="10">
        <Label FontSize="50" d:Content="Home" Content="{Binding Name}"></Label>
    </Grid>
</UserControl>
